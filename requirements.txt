# Core WebSocket and OCPP dependencies
websockets>=12.0
ocpp>=2.1.0  # Supports OCPP 1.6 and 2.0.1/2.1
uvloop>=0.19.0

# Redis removed for simplification - can be added back later
# redis>=5.0.1
# hiredis>=2.2.3

# Async utilities
aiofiles>=23.2.1
aiohttp>=3.9.3
httpx>=0.25.0

# Data serialization and validation
pydantic>=2.5.0
msgpack>=1.0.7

# Monitoring and observability
prometheus-client>=0.19.0
structlog>=24.1.0
colorlog>=6.8.0

# Async utilities
aiocontextvars>=0.2.2
asyncio-mqtt>=0.16.1

# Security and TLS
cryptography>=41.0.8
pyopenssl>=23.3.0

# Configuration management
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Development and testing
pytest>=7.4.4
pytest-asyncio>=0.23.2
pytest-mock>=3.12.0
black>=23.12.0
isort>=5.13.2
mypy>=1.8.0

# Performance monitoring
psutil>=5.9.6
py-spy>=0.3.14

# Resilience and error handling
backoff>=2.2.1
tenacity>=8.2.3

# Supabase integration
supabase>=2.3.0
postgrest>=0.13.0
asyncpg>=0.29.0

# TimescaleDB integration
psycopg2-binary>=2.9.7
sqlalchemy>=2.0.0
pandas>=2.0.0
numpy>=1.24.0

# Optimization (MILP solver)
# Per PRD_v2.md Section 8.2: Gurobi primary, HiGHS fallback
pyomo>=6.7.0
gurobipy>=11.0.0  # Primary solver (requires license)
highspy>=1.5.0  # Fallback solver (open-source)

# Machine Learning (Surrogate Model)
scikit-learn>=1.3.0
gpytorch>=1.4.0  # Gaussian Process for energy consumption prediction
torch>=2.0.0  # Required by gpytorch
